# Tabla del numero que decidas
def tabla_1():
    ntabla=input("Dime el numero de tabla")
    for cont in range (1,11):
        print(str(ntabla)+"*"+str(cont)+"="+str(int(ntabla)*cont))
        
tabla_1()
